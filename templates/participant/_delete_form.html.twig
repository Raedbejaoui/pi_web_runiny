<form method="post" action="{{ path('app_participant_delete', {'idParti': participant.idParti}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.idParti) }}">
    <button class="btn">Delete</button>
</form>
